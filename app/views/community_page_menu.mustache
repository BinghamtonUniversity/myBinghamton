
	<div class="col-md-12">
		<ul class="nav nav-boxed nav-justified">
	{{#items}}

	<li class="page_{{id}} {{device_class}} {{^groups}}group_all{{/groups}}" data-groups="{{#groups}}{{groups}}{{/groups}}">
	<a href="/community/{{group_slug}}/{{slug}}" data-page="{{id}}">{{name}}</a>	
	</li>

	{{/items}}
		</ul>
	</div>